[pytest]
testpaths = .
python_files = test_*.py
addopts = -ra -q
# archived/ holds pre-split originals kept for reference only -- never collected (would
# otherwise duplicate-run every test now split into test_*_{windows,linux,cloud}.py).
norecursedirs = archived .git __pycache__ .pytest_cache
